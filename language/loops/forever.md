# Forever Loops
**STATUS**: Implemented

Forever loops are just infinite for loops. They are equivalent to using `for(;;)` or `while(true)`. 

    forever do
        [[ _code_ ]]
    end

    # or 

    _code_ forever 
